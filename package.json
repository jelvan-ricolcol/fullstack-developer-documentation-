{
  "name": "workers-example",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=neutral --target=es2022 --outfile=dist/index.js",
    "dev": "wrangler dev --local",
    "start": "wrangler dev",
    "typecheck": "tsc --noEmit",
    "test": "vitest",
    "lint": "echo \"no lint configured\""
  },
  "dependencies": {
    "itty-router": "^3.0.12",
    "itty-router-extras": "^2.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.19.0",
    "typescript": "^5.0.0",
    "wrangler": "^2.11.0",
    "vitest": "^0.34.0",
    "@cloudflare/workers-types": "^4.2026.0"
  }
}
